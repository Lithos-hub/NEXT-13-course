# Next.js Jira Clone App

Get the database:

```
docker-compose up -d
```

* -d means __detached__

MongoDB local URL:

```
mongodb://localhost:27017/tasksdb
```

## Setup environment variables

Rename the file __.env.example.__ to __.env.__